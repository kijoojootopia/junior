# 프로젝트 개발 및 파일 격리 규칙 (AI 작업 지침)

## 1. 기본 원칙 (기존 코드 보존 및 무충돌)
- **기존 코드 임의 수정/삭제 금지:** 기존 파일(`app.py`, `templates/base.html`, `templates/index.html` 등)에 작성된 기존 비즈니스 로직, 스타일, 함수를 임의로 리팩터링하거나 삭제하지 마세요.
- **철저한 파일 격리:** 새로운 기능을 추가할 때는 기존 파일의 코드를 늘리지 말고, 독립된 신규 파일을 생성하여 작성하세요.

## 2. 폴더별 아키텍처 및 저장 규칙
- **비즈니스 로직 및 계산 (services/):**
  - 모든 KPI 계산, 데이터 전처리, 비즈니스 로직은 `services/` 폴더 내에 전용 신규 파일(예: `services/export_service.py`)로 분리해 작성하세요.
- **화면 템플릿 (templates/):**
  - 신규 화면이나 탭 컴포넌트는 `templates/` 안에 독립 템플릿 파일(예: `templates/export_dashboard.html`)로 분리해 생성하세요.
  - 기존 템플릿(`index.html` 또는 `base.html`)은 신규 화면으로 연결되는 탭/링크 버튼 최소 1줄 외에는 수정하지 마세요.
- **정적 파일 (static/):**
  - 신규 스타일은 `static/css/`, 신규 자바스크립트는 `static/js/` 안에 전용 신규 파일로 분리하세요. 기존 CSS/JS를 덮어쓰지 마세요.
- **데이터 저장소 (data/):**
  - 샘플 데이터, 매핑 데이터, 더미 JSON/CSV 등은 `data/` 폴더 안에 보관하세요.
- **문서화 (readme.md):**
  - 기능 추가나 파일 생성이 완료되면 항상 `readme.md`에 신규 기능 개요, 주요 지표(KPI) 산출 방식, 추가된 파일 목록을 마크다운으로 정리해 반영하세요.

## 3. 작업 전 사전 브리핑 의무
- 코드를 작성하거나 파일을 수정하기 전에, 반드시 **(1) 신규 생성할 파일 목록**과 **(2) 최소 수정할 기존 파일 목록 및 수정 내용**을 한국어로 간략히 정리해 보고한 뒤 작업을 시작하세요.